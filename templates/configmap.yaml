---
apiVersion: v1
kind: ConfigMap
metadata:
  name: vk-config
  namespace: {{ .Values.namespace.name }}
  labels:
    app.kubernetes.io/part-of: vibe-kanban
data:
  PUBLIC_BASE_URL: {{ .Values.config.publicBaseUrl | quote }}
  RELAY_PUBLIC_URL: {{ .Values.config.relayPublicUrl | quote }}
